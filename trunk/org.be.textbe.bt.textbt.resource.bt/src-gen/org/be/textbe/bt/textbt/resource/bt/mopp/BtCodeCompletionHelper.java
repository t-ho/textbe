/**
 * <copyright>
 * </copyright>
 *
 * 
 */
package org.be.textbe.bt.textbt.resource.bt.mopp;

// A CodeCompletionHelper can be used to derive completion proposals for partial
// documents. It runs the parser generated by EMFText in a special mode (i.e., the
// rememberExpectedElements mode). Based on the elements that are expected by the
// parser for different regions in the document, valid proposals are computed.
public class BtCodeCompletionHelper {
	
	// Computes a set of proposals for the given document assuming the cursor is
	// at 'cursorOffset'. The proposals are derived using the meta information, i.e.,
	// the generated language plug-in.
	//
	// @param metaInformation
	// @param content the documents content
	// @param cursorOffset
	// @return
	public org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal[] computeCompletionProposals(org.be.textbe.bt.textbt.resource.bt.IBtTextResource originalResource, String content, int cursorOffset) {
		org.eclipse.emf.ecore.resource.ResourceSet resourceSet = new org.eclipse.emf.ecore.resource.impl.ResourceSetImpl();
		// the shadow resource needs the same URI because reference resolvers may use the URI to resolve external references
		org.be.textbe.bt.textbt.resource.bt.IBtTextResource resource = (org.be.textbe.bt.textbt.resource.bt.IBtTextResource) resourceSet.createResource(originalResource.getURI());
		java.io.ByteArrayInputStream inputStream = new java.io.ByteArrayInputStream(content.getBytes());
		org.be.textbe.bt.textbt.resource.bt.mopp.BtMetaInformation metaInformation = resource.getMetaInformation();
		org.be.textbe.bt.textbt.resource.bt.IBtTextParser parser = metaInformation.createParser(inputStream, null);
		org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[] expectedElements = parseToExpectedElements(parser, resource);
		if (expectedElements == null) {
			return new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal[0];
		}
		if (expectedElements.length == 0) {
			return new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal[0];
		}
		java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedAfterCursor = java.util.Arrays.asList(getElementsExpectedAt(expectedElements, cursorOffset));
		java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedBeforeCursor = java.util.Arrays.asList(getElementsExpectedAt(expectedElements, cursorOffset - 1));
		setPrefixes(expectedAfterCursor, content, cursorOffset);
		setPrefixes(expectedBeforeCursor, content, cursorOffset);
		// first we derive all possible proposals from the set of elements that are expected at the cursor position
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> allProposals = new java.util.LinkedHashSet<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>();
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> rightProposals = deriveProposals(expectedAfterCursor, content, resource, cursorOffset);
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> leftProposals = deriveProposals(expectedBeforeCursor, content, resource, cursorOffset);
		// second, the set of left proposals (i.e., the ones before the cursor) is
		// checked for emptiness. if the set is empty, the right proposals (i.e., 
		// the ones after the cursor are removed, because it does not make sense to
		// propose them until the element before the cursor was completed
		allProposals.addAll(leftProposals);
		if (leftProposals.isEmpty()) {
			allProposals.addAll(rightProposals);
		}
		// third, the proposals are sorted according to their relevance
		// proposals that matched the prefix are preferred over ones that did not
		// afterward proposals are sorted alphabetically
		final java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> sortedProposals = new java.util.ArrayList<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>(allProposals);
		java.util.Collections.sort(sortedProposals);
		return sortedProposals.toArray(new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal[sortedProposals.size()]);
	}
	
	public org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[] parseToExpectedElements(org.be.textbe.bt.textbt.resource.bt.IBtTextParser parser, org.be.textbe.bt.textbt.resource.bt.IBtTextResource resource) {
		final java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedElements = parser.parseToExpectedElements(null, resource);
		if (expectedElements == null) {
			return new org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[0];
		}
		removeDuplicateEntries(expectedElements);
		removeInvalidEntriesAtEnd(expectedElements);
		return expectedElements.toArray(new org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[expectedElements.size()]);
	}
	
	private void removeDuplicateEntries(java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedElements) {
		for (int i = 0; i < expectedElements.size() - 1; i++) {
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal elementAtIndex = expectedElements.get(i);
			for (int j = i + 1; j < expectedElements.size();) {
				org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal elementAtNext = expectedElements.get(j);
				if (elementAtIndex.equals(elementAtNext) && elementAtIndex.getStartExcludingHiddenTokens() == elementAtNext.getStartExcludingHiddenTokens()) {
					expectedElements.remove(j);
				} else {
					j++;
				}
			}
		}
	}
	
	private void removeInvalidEntriesAtEnd(java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedElements) {
		for (int i = 0; i < expectedElements.size() - 1;) {
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal elementAtIndex = expectedElements.get(i);
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal elementAtNext = expectedElements.get(i + 1);
			if (elementAtIndex.getStartExcludingHiddenTokens() == elementAtNext.getStartExcludingHiddenTokens() && shouldRemove(elementAtIndex.getFollowSetID(), elementAtNext.getFollowSetID())) {
				expectedElements.remove(i + 1);
			} else {
				i++;
			}
		}
	}
	
	public boolean shouldRemove(int followSetID1, int followSetID2) {
		return followSetID1 != followSetID2;
	}
	
	private String findPrefix(java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedElements, org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedAtCursor, String content, int cursorOffset) {
		if (cursorOffset < 0) {
			return "";
		}
		int end = 0;
		for (org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedElement : expectedElements) {
			if (expectedElement == expectedAtCursor) {
				final int start = expectedElement.getStartExcludingHiddenTokens();
				if (start >= 0  && start < Integer.MAX_VALUE) {
					end = start;
				}
				break;
			}
		}
		end = Math.min(end, cursorOffset);
		final String prefix = content.substring(end, Math.min(content.length(), cursorOffset));
		return prefix;
	}
	
	private java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> deriveProposals(java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedElements, String content, org.be.textbe.bt.textbt.resource.bt.IBtTextResource resource, int cursorOffset) {
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> resultSet = new java.util.HashSet<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>();
		for (org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedElement : expectedElements) {
			resultSet.addAll(deriveProposals(expectedElement, content, resource, cursorOffset));
		}
		return resultSet;
	}
	
	private java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> deriveProposals(org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedTerminal, String content, org.be.textbe.bt.textbt.resource.bt.IBtTextResource resource, int cursorOffset) {
		org.be.textbe.bt.textbt.resource.bt.mopp.BtMetaInformation metaInformation = resource.getMetaInformation();
		org.be.textbe.bt.textbt.resource.bt.IBtLocationMap locationMap = resource.getLocationMap();
		org.be.textbe.bt.textbt.resource.bt.IBtExpectedElement expectedElement = (org.be.textbe.bt.textbt.resource.bt.IBtExpectedElement) expectedTerminal.getTerminal();
		if (expectedElement instanceof org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedCsString) {
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedCsString csString = (org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedCsString) expectedElement;
			return deriveProposal(csString, content, expectedTerminal.getPrefix(), cursorOffset);
		} else if (expectedElement instanceof org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedStructuralFeature) {
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedStructuralFeature expectedFeature = (org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedStructuralFeature) expectedElement;
			org.eclipse.emf.ecore.EStructuralFeature feature = expectedFeature.getFeature();
			org.eclipse.emf.ecore.EClassifier featureType = feature.getEType();
			java.util.List<org.eclipse.emf.ecore.EObject> elementsAtCursor = locationMap.getElementsAt(cursorOffset);
			org.eclipse.emf.ecore.EObject container = null;
			// we need to skip the proxy elements at the cursor, because they are not the container for the reference we try to complete
			for (int i = 0; i < elementsAtCursor.size(); i++) {
				container = elementsAtCursor.get(i);
				if (!container.eIsProxy()) {
					break;
				}
			}
			if (feature instanceof org.eclipse.emf.ecore.EReference) {
				org.eclipse.emf.ecore.EReference reference = (org.eclipse.emf.ecore.EReference) feature;
				if (featureType instanceof org.eclipse.emf.ecore.EClass) {
					if (reference.isContainment()) {
						// the FOLLOW set should contain only non-containment references
						assert false;
					} else {
						return handleNCReference(metaInformation, container, reference, expectedTerminal.getPrefix());
					}
				}
			} else if (feature instanceof org.eclipse.emf.ecore.EAttribute) {
				org.eclipse.emf.ecore.EAttribute attribute = (org.eclipse.emf.ecore.EAttribute) feature;
				if (featureType instanceof org.eclipse.emf.ecore.EEnum) {
					org.eclipse.emf.ecore.EEnum enumType = (org.eclipse.emf.ecore.EEnum) featureType;
					return deriveProposals(expectedTerminal, enumType, content, cursorOffset);
				} else {
					// handle EAttributes (derive default value depending on
					// the type of the attribute, figure out token resolver, and
					// call deResolve())
					return handleAttribute(metaInformation, expectedFeature, container, attribute, expectedTerminal.getPrefix());
				}
			} else {
				// there should be no other subclass of EStructuralFeature
				assert false;
			}
		} else {
			// there should be no other class implementing IExpectedElement
			assert false;
		}
		return java.util.Collections.emptyList();
	}
	
	private java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> deriveProposals(org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedElement, org.eclipse.emf.ecore.EEnum enumType, String content, int cursorOffset) {
		java.util.Collection<org.eclipse.emf.ecore.EEnumLiteral> enumLiterals = enumType.getELiterals();
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> result = new java.util.HashSet<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>();
		for (org.eclipse.emf.ecore.EEnumLiteral literal : enumLiterals) {
			String proposal = literal.getLiteral();
			String prefix = expectedElement.getPrefix();
			if (matches(proposal, prefix)) {
				result.add(new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal(proposal, prefix, !"".equals(prefix), true));
			}
		}
		return result;
	}
	
	private java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> handleNCReference(org.be.textbe.bt.textbt.resource.bt.mopp.BtMetaInformation metaInformation, org.eclipse.emf.ecore.EObject container, org.eclipse.emf.ecore.EReference reference, java.lang.String prefix) {
		// proposals for non-containment references are derived by calling the
		// reference resolver switch in fuzzy mode.
		org.be.textbe.bt.textbt.resource.bt.IBtReferenceResolverSwitch resolverSwitch = metaInformation.getReferenceResolverSwitch();
		org.be.textbe.bt.textbt.resource.bt.IBtReferenceResolveResult<org.eclipse.emf.ecore.EObject> result = new org.be.textbe.bt.textbt.resource.bt.mopp.BtReferenceResolveResult<org.eclipse.emf.ecore.EObject>(true);
		resolverSwitch.resolveFuzzy(prefix, container, reference, 0, result);
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.IBtReferenceMapping<org.eclipse.emf.ecore.EObject>> mappings = result.getMappings();
		if (mappings != null) {
			java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> resultSet = new java.util.HashSet<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>();
			for (org.be.textbe.bt.textbt.resource.bt.IBtReferenceMapping<org.eclipse.emf.ecore.EObject> mapping : mappings) {
				final String identifier = mapping.getIdentifier();
				// the proposal can be added without checking the prefix because this is
				// performed by the reference resolvers
				resultSet.add(new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal(identifier, prefix, true, true));
			}
			return resultSet;
		}
		return java.util.Collections.emptyList();
	}
	
	private java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> handleAttribute(org.be.textbe.bt.textbt.resource.bt.mopp.BtMetaInformation metaInformation, org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedStructuralFeature expectedFeature, org.eclipse.emf.ecore.EObject container, org.eclipse.emf.ecore.EAttribute attribute, java.lang.String prefix) {
		java.lang.Object defaultValue = getDefaultValue(attribute);
		if (defaultValue != null) {
			org.be.textbe.bt.textbt.resource.bt.IBtTokenResolverFactory tokenResolverFactory = metaInformation.getTokenResolverFactory();
			String tokenName = expectedFeature.getTokenName();
			if (tokenName != null) {
				org.be.textbe.bt.textbt.resource.bt.IBtTokenResolver tokenResolver = tokenResolverFactory.createTokenResolver(tokenName);
				if (tokenResolver != null) {
					String defaultValueAsString = tokenResolver.deResolve(defaultValue, attribute, container);
					java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> resultSet = new java.util.HashSet<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>();
					if (matches(defaultValueAsString, prefix)) {
						resultSet.add(new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal(defaultValueAsString, prefix, !"".equals(prefix), true));
					}
					return resultSet;
				}
			}
		}
		return java.util.Collections.emptyList();
	}
	
	private java.lang.Object getDefaultValue(org.eclipse.emf.ecore.EAttribute attribute) {
		String typeName = attribute.getEType().getName();
		if ("EString".equals(typeName)) {
			return "some" + org.be.textbe.bt.textbt.resource.bt.util.BtStringUtil.capitalize(attribute.getName());
		}
		return attribute.getDefaultValue();
	}
	
	private java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> deriveProposal(org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedCsString csString, String content, String prefix, int cursorOffset) {
		String proposal = csString.getValue();
		java.util.Collection<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal> result = new java.util.HashSet<org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal>();
		if (matches(proposal, prefix)) {
			result.add(new org.be.textbe.bt.textbt.resource.bt.mopp.BtCompletionProposal(proposal, prefix, !"".equals(prefix), false));
		}
		return result;
	}
	
	// for each given expected elements the prefix is calculated
	// the prefix depends on the current document content, the cursor position, and
	// the position where the element is expected
	private void setPrefixes(java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedElements, String content, int cursorOffset) {
		if (cursorOffset < 0) {
			return;
		}
		for (org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedElement : expectedElements) {
			java.lang.String prefix = findPrefix(expectedElements, expectedElement, content, cursorOffset);
			expectedElement.setPrefix(prefix);
		}
	}
	
	public org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[] getElementsExpectedAt(org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[] allExpectedElements, int cursorOffset) {
		java.util.List<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal> expectedAtCursor = new java.util.ArrayList<org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal>();
		for (int i = 0; i < allExpectedElements.length; i++) {
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal expectedElement = allExpectedElements[i];
			int startIncludingHidden = expectedElement.getStartIncludingHiddenTokens();
			int end = getEnd(allExpectedElements, i);
			if (cursorOffset >= startIncludingHidden && cursorOffset <= end) {
				expectedAtCursor.add(expectedElement);
			}
		}
		return expectedAtCursor.toArray(new org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[expectedAtCursor.size()]);
	}
	
	private int getEnd(org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal[] allExpectedElements, int indexInList) {
		org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal elementAtIndex = allExpectedElements[indexInList];
		int startIncludingHidden = elementAtIndex.getStartIncludingHiddenTokens();
		int startExcludingHidden = elementAtIndex.getStartExcludingHiddenTokens();
		for (int i = indexInList + 1; i < allExpectedElements.length; i++) {
			org.be.textbe.bt.textbt.resource.bt.mopp.BtExpectedTerminal elementAtI = allExpectedElements[i];
			int startIncludingHiddenForI = elementAtI.getStartIncludingHiddenTokens();
			int startExcludingHiddenForI = elementAtI.getStartExcludingHiddenTokens();
			if (startIncludingHidden != startIncludingHiddenForI || startExcludingHidden != startExcludingHiddenForI) {
				return startIncludingHiddenForI - 1;
			}
		}
		return Integer.MAX_VALUE;
	}
	
	private boolean matches(java.lang.String proposal, java.lang.String prefix) {
		return (proposal.startsWith(prefix) || org.be.textbe.bt.textbt.resource.bt.util.BtStringUtil.matchCamelCase(prefix, proposal) != null) && !proposal.equals(prefix);
	}
	
}
